# DiagramAI Version Environment Variables
# Copy this file to .env.local for local development
# These variables will be used for version tracking in the application

# Application version (semantic versioning recommended)
APP_VERSION=1.0.45-dev

# Build date (automatically set by CI/CD, manual for local dev)
BUILD_DATE=2025-01-27 12:00:00

# Git commit hash (automatically set by CI/CD, manual for local dev)
GIT_COMMIT=unknown

# Instructions:
# 1. Copy this file: cp .env.version.example .env.local
# 2. Update the values as needed
# 3. Run: npm run dev
# 
# For Docker development:
# 1. Set environment variables: source scripts/set-version.sh
# 2. Run: docker compose up --build
#
# Note: CI/CD pipeline automatically sets these values during builds
